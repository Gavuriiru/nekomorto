---
import DashboardIslandApp from "../../components/react/DashboardIslandApp";
import DashboardHostLayout from "../../layouts/DashboardHostLayout.astro";
---

<DashboardHostLayout title="Dashboard">
  <DashboardIslandApp
    client:only="react"
    initialCurrentUser={Astro.locals.nekomata?.currentUser || null}
    initialSettings={Astro.locals.nekomata?.siteSettings || null}
  />
</DashboardHostLayout>
